
name: CI_Hieu

on:
  push:
    branches: [ "CIhieu" ]

  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
     # - uses: actions/checkout@v4

      # Runs a single command using the runners shell
     # - name: Run a one-line script
      #  run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run CI web
        run: |
          mkdir -p ~/.ssh
          touch ~/.ssh/id_rsa
          echo "${{ secrets.SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan 34.143.236.100 >> ~/.ssh/known_hosts
          ssh -i  ~/.ssh/id_rsa hieu@34.143.236.100 sh -c "
            set -x
            mkdir -p /home/hieu/CIhieu
            echo 'git clone code neu chua co'
            #git clone git@github.com:tronghieu0511/todb17_danhsachlop_hieu_copy.git
            echo 'git pull'
            echo 'compose up'
          "
